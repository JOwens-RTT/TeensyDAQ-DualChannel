#ifndef TEENSY_DAQ_INCLUDE_PROJ_ALIASES_H
#define TEENSY_DAQ_INCLUDE_PROJ_ALIASES_H

#include <Arduino.h>

using TimeStamp = volatile uint32_t;
using ADCValue = volatile uint16_t;
using Pin = uint8_t;

#endif        // TEENSY_DAQ_INCLUDE_PROJ_ALIASES_H